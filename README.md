# Git-Task

## Restore in Git
A Git command used to discard changes in your working directory and bring files back to the last committed state.

```
git restore file.txt  //remove your local edits and resets file.txt to the last commit

git restore . //restore all files

gi restore --staged file.txt //move the file from staging area back to working directory
```





